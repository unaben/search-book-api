# Dependencies
node_modules/

# Build output
dist/

# Log files — generated at runtime, not committed
logs/

# Environment variables — never commit real values
.env
.env.local
# .env.example is intentionally committed — it shows what vars are needed

# TypeScript cache
*.tsbuildinfo

# OS files
.DS_Store

# Test coverage — generated at runtime
coverage/